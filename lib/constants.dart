import 'package:flutter/material.dart';

// Colos that use in our app
const Color kSecondaryColor = Color(0xFFFE6D8E);
const Color kTextColor = Color(0xFF12153D);
const Color kTextLightColor = Color(0xFF9A9BB2);
const Color kFillStarColor = Color(0xFFFCC419);

const double kDefaultPadding = 20.0;

const BoxShadow kDefaultShadow = BoxShadow(
  offset: Offset(0, 4),
  blurRadius: 4,
  color: Colors.black26,
);
